<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="acs_video_call_invitaion" model="mail.template">
            <field name="name">Video Consultation Invitation</field>
            <field name="email_from">${user.email|safe}</field>
            <field name="email_to">${object.patient_id.partner_id.email|safe}</field>
            <field name="partner_to">${object.get_partner_ids()}</field>
            <field name="subject">Video Consultation Invitation</field>
            <field name="model_id" ref="acs_hms.model_hms_appointment"/>
            <field name="lang">${object.patient_id.partner_id.lang}</field>
            <field name="body_html" type="html">
<div style="padding:0px;margin:auto;background: #FFFFFF repeat top /100%;color:#777777">
    % set call_date = format_datetime(object.date, tz=(object.physician_id.tz or "UTC"), dt_format=(object.physician_id.lang.date_format))
    <p>Hello Dear ${object.patient_id.name},</p><br/>

    <p>Your Video Consultation with ${object.physician_id.name} have been Scheduled with following details.</p>
    <ul>
        <li>
            <p>Appointment Number: ${object.name}</p><br/>
        </li>
        % if object.video_call_id:
        <li>
            <p>Meeting Link: <a href="${object.video_call_id.meeting_link}">${object.video_call_id.meeting_link}</a></p><br/>
        </li>
        % endif
        % if object.video_call_id.password:
        <li>
            <p>Meeting Password: ${object.video_call_id.password}</p><br/>
        </li>
        % endif
        % if object.date:
        <li>
            <p>Date: ${call_date} (Timezone: ${object.physician_id.tz or "UTC"})</p><br/>
        </li>
        % endif
        % if object.physician_id:
        <li>
            <p>Physician: ${object.physician_id.name}</p><br/>
        </li>
        % endif
    </ul>

    <p>Please feel free to call anytime for further information or any query.</p>

    <p>Best regards.</p><br/>
</div>
            </field>
        </record>

    </data>
</odoo>
