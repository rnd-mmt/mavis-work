<?xml version="1.0"?>
<odoo>

    <!-- Appointment View -->
    <record id="acs_video_call_form" model="ir.ui.view">
        <field name="name">hmsacs.video.call.form</field>
        <field name="model">acs.video.call</field>
        <field name="inherit_id" ref="acs_video_call.acs_video_call_form"/>
        <field name="arch" type="xml">
            <field name="partner_ids" position="before">
                <field name="appointment_id"/>
            </field>
        </field>
    </record>

</odoo>